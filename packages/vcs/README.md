# VCS Packages

English | [中文](README.zh.md)

Version-control-facing packages: the model-facing tool suite over the filesystem and shell seams.

| Package | Role |
|---|---|
| [`tool-vcs`](tool-vcs/) | Model-facing `repo_map` (bounded source-tree scan with symbols and git status markers) and `git_workflow` (status / diff / commit / rollback) over `ctx.fs` and `ctx.shell`. |
