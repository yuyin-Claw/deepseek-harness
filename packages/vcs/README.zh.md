# VCS 包

[English](README.md) | 中文

面向版本控制的包：构建在文件系统与 shell 能力之上的面向模型工具套件。

| 包 | 角色 |
|---|---|
| [`tool-vcs`](tool-vcs/) | 面向模型的 `repo_map`（带符号与 git 状态标记的有界源码树扫描）与 `git_workflow`（status / diff / commit / rollback），走 `ctx.fs` 与 `ctx.shell`。 |
