/**
 * Voice input via Web Speech API into the composer draft, node half. Deliberately empty: client-only feature.
 */

/** Host plugin body — no host-side capability yet. */
export function apply(): void {}
