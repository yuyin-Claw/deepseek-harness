/**
 * Node half of the side workspace UI plugin: the feature is browser-only, so
 * this face contributes no behavior and declares no service dependencies.
 */
export const inject: string[] = []

/** Node-face apply: intentionally empty. */
export function apply(): void {}
